#---
#- name: Pull Docker Image of Calculator
#  hosts: all
#  vars:
#    ansible_python_interpreter: /usr/bin/python3
#  tasks:
#    - name: Pull image
#      docker_image:
#        name: ketakitam/calculator:latest
#        source: pull
#    - name: Start docker service
#      service:
#        name: docker
#        state: started
#    - name: Running container
#      shell: docker run -it -d --name Calculator ketakitam/calculator
#

---
- name: Pull Docker Image of Calculator
  hosts: all

  tasks:
    - name: Pull Image
      shell: docker pull ketakitam/calculator:latest

    - name: Run the container
      shell: docker run -it -d --name Calculator ketakitam/calculator